Our domain model should not know how they are being store in the database.
They should have only one responsibility that is representing the domain knowledge.
Because of that we creat a model to represent it on database, a isolated one.
So our database is a detail as well, should not impact in our domain.

Only one entity by aggregate.
